<UserControl x:Class="launcher.AdvancedMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:launcher"
             mc:Ignorable="d"
             d:DesignHeight="707" d:DesignWidth="1340">
    <UserControl.Resources>
        <Style x:Key="FocusVisual3">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background2" Color="#FFDDDDDD" />
        <SolidColorBrush x:Key="Button.Static.Border2" Color="#FF707070" />
        <SolidColorBrush x:Key="Button.MouseOver.Background2" Color="#FFBEE6FD" />
        <SolidColorBrush x:Key="Button.MouseOver.Border2" Color="#FF3C7FB1" />
        <SolidColorBrush x:Key="Button.Pressed.Background2" Color="#FFC4E5F6" />
        <SolidColorBrush x:Key="Button.Pressed.Border2" Color="#FF2C628B" />
        <SolidColorBrush x:Key="Button.Disabled.Background2" Color="#FFF4F4F4" />
        <SolidColorBrush x:Key="Button.Disabled.Border2" Color="#FFADB2B5" />
        <SolidColorBrush x:Key="Button.Disabled.Foreground2" Color="#FF838383" />
        <Style x:Key="ToggleButtonCombo" TargetType="{x:Type ToggleButton}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual3}" />
            <Setter Property="Background" Value="{StaticResource Button.Static.Background2}" />
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border2}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Button.IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="Transparent" />
                                <Setter Property="BorderBrush" TargetName="border" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="Transparent" />
                                <Setter Property="BorderBrush" TargetName="border" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="Transparent" />
                                <Setter Property="BorderBrush" TargetName="border" Value="Transparent" />
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="{StaticResource Button.Disabled.Foreground2}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBoxBorder" Background="{StaticResource BranchComboBorderColor}" BorderThickness="1" CornerRadius="4" BorderBrush="{StaticResource AccentColor}">
                                <DockPanel LastChildFill="True">
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Left"
                                      Margin="5,0,0,0"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" />
                                    <ToggleButton Style="{DynamicResource ToggleButtonCombo}" DockPanel.Dock="Right" Width="249" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                  BorderThickness="0" Background="Transparent" HorizontalAlignment="Right" Padding="210,0,0,0">
                                        <Path Data="M 0 0 L 10 10 L 20 0 Z"
                              Fill="{StaticResource PrimaryTextColor}" VerticalAlignment="Center" HorizontalAlignment="Right" />
                                    </ToggleButton>
                                </DockPanel>
                            </Border>

                            <Popup Name="PART_Popup" MaxHeight="400"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   Placement="Bottom"
                   PlacementTarget="{Binding ElementName=PART_Popup}"
                   AllowsTransparency="True"
                   PopupAnimation="Fade"
                   VerticalOffset="38">
                                <Border Background="{StaticResource BranchComboPopupColor}" BorderBrush="{StaticResource BranchComboPopupColor}" BorderThickness="0" CornerRadius="5" Width="275">
                                    <ScrollViewer>
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ComboBoxBorder" Property="BorderBrush" Value="{StaticResource BranchComboHoverColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FocusVisual2">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewHover.Background" Color="#1F26A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewHover.Border" Color="#A826A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelected.Background" Color="#3D26A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelected.Border" Color="#FF26A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelectedHover.Background" Color="#2E0080FF" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelectedHover.Border" Color="#99006CD9" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelectedNoFocus.Background" Color="#3DDADADA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewSelectedNoFocus.Border" Color="#FFDADADA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewFocus.Border" Color="#FF26A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewHoverFocus.Background" Color="#5426A0DA" />
        <SolidColorBrush x:Key="ComboBoxItem.ItemsviewHoverFocus.Border" Color="#FF26A0DA" />
        <Style x:Key="ComboBoxItemContainerStyle" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Padding" Value="4,1" />
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual2}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="Bd" Background="#143a52" BorderBrush="#143a52" BorderThickness="0" Padding="10" SnapsToDevicePixels="true" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsKeyboardFocused" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource BranchComboMouseOverColor}" />
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboMouseOverColor}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True" />
                                    <Condition Property="IsMouseOver" Value="False" />
                                    <Condition Property="IsKeyboardFocused" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource BranchComboSelectedColor}" />
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboSelectedColor}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource BranchComboSelectedMouseOverColor}" />
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboSelectedMouseOverColor}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="True" />
                                    <Condition Property="IsMouseOver" Value="False" />
                                    <Condition Property="IsKeyboardFocused" Value="False" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource BranchComboSelectedColor}" />
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboSelectedColor}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False" />
                                    <Condition Property="IsMouseOver" Value="False" />
                                    <Condition Property="IsKeyboardFocused" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboSelectedBorderColor}" />
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="False" />
                                    <Condition Property="IsMouseOver" Value="True" />
                                    <Condition Property="IsKeyboardFocused" Value="True" />
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="Bd" Value="{StaticResource BranchComboSelectedMouseOverColor}" />
                                <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource BranchComboSelectedMouseOverColor}" />
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="Button.Static.Background" Color="#FFDDDDDD" />
        <SolidColorBrush x:Key="Button.Static.Border" Color="#FF707070" />
        <Style x:Key="SettingsButton" TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}" />
            <Setter Property="Background" Value="{StaticResource Button.Static.Background}" />
            <Setter Property="BorderBrush" Value="{StaticResource Button.Static.Border}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="true" CornerRadius="5">
                            <ContentPresenter x:Name="contentPresenter" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDefaulted" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource SettingsNavButtonColorHover}" />
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource SettingsNavButtonColorHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource SettingsNavButtonColorHover}" />
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource SettingsNavButtonColorHover}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Background" TargetName="border" Value="{StaticResource SettingsNavButtonColorNormal}" />
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource SettingsNavButtonColorNormal}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SwitchStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <ControlTemplate.Resources>
                            <Storyboard x:Key="OnChecking">
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="slider" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.1000000" Value="25" />
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                            <Storyboard x:Key="OnUnchecking">
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="slider" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.1000000" Value="2" />
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                        </ControlTemplate.Resources>
                        <DockPanel x:Name="dockPanel">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" ContentTemplate="{TemplateBinding ContentTemplate}" RecognizesAccessKey="True" VerticalAlignment="Center" />
                            <Grid>
                                <Border x:Name="BackgroundBorder" Background="Transparent" BorderBrush="{StaticResource SwitchColor}" BorderThickness="1" CornerRadius="12" Height="27" Width="50" />
                                <Border Background="#FF52AAF2" HorizontalAlignment="Left" x:Name="slider" Width="23" Height="23" BorderThickness="1" CornerRadius="10" RenderTransformOrigin="0.5,0.5" Margin="0">
                                    <Border.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                            <SkewTransform AngleX="0" AngleY="0" />
                                            <RotateTransform Angle="0" />
                                            <TranslateTransform X="2" Y="0" />
                                        </TransformGroup>
                                    </Border.RenderTransform>
                                    <Image x:Name="checkImage" Source="/assets/icons8-x-50.png" />
                                </Border>
                            </Grid>
                        </DockPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Trigger.ExitActions>
                                    <BeginStoryboard Storyboard="{StaticResource OnUnchecking}" x:Name="OnUnchecking_BeginStoryboard" />
                                </Trigger.ExitActions>
                                <Trigger.EnterActions>
                                    <BeginStoryboard Storyboard="{StaticResource OnChecking}" x:Name="OnChecking_BeginStoryboard" />
                                </Trigger.EnterActions>
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="{StaticResource SwitchColor}" />
                                <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{StaticResource SwitchColor}" />
                                <Setter TargetName="slider" Property="Background" Value="{StaticResource SwitchBackgroundColor}" />
                                <Setter TargetName="checkImage" Property="Source" Value="/assets/icons8-check-48.png" />
                                <Setter TargetName="checkImage" Property="Width" Value="15" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <!-- ToDo: Add Style for Isenabled == False -->
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AdvancedTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
            <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderColor}" />
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="AllowDrop" Value="true" />
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryTextColor}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <Border x:Name="border" CornerRadius="5" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBoxColorHover}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBoxColorHover}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsInactiveSelectionHighlightEnabled" Value="true" />
                        <Condition Property="IsSelectionActive" Value="false" />
                    </MultiTrigger.Conditions>
                    <Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
                </MultiTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="FocusVisual1">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" StrokeDashArray="1 2" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" SnapsToDevicePixels="true" StrokeThickness="1" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="TextBox.Static.Background" Color="#FFFFFFFF" />
        <Style x:Key="ComboBoxEditableTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="AllowDrop" Value="true" />
            <Setter Property="MinWidth" Value="0" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
            <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Border Background="{StaticResource SecondaryColor}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100" />
                <RowDefinition Height="1*" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" x:Name="BackBtn" Style="{DynamicResource SettingsButton}" Width="60" Height="60" FontFamily="Bahnschrift SemiBold" FontSize="18" Background="Transparent" BorderBrush="Transparent" Click="BackButton_Click" Margin="23,23,1257,17" Cursor="Hand">
                <Grid Width="60" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="/assets/icons8-left-arrow-100.png" Width="30" Height="30" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>
            </Button>
            <Rectangle Grid.Row="0" Fill="{StaticResource SeperatorColor}" Margin="94,23,1240,17" IsHitTestVisible="False" Height="60" />
            <TextBlock Grid.Row="0" Text="Advanced Settings" VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="{StaticResource PrimaryTextColor}" FontFamily="Bahnschrift SemiBold" FontSize="36" Width="302" Margin="111,31,0,0" />

            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="380" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0">
                    <CheckBox x:Name="Cheats" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Enable Cheats" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,10,0" Height="20" ></TextBlock>
                    <TextBlock Text="Enables the ability to use cheats ingame" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,10,0" Height="16" />
                </Grid>
                <Grid Grid.Row="1" Grid.Column="0">
                    <CheckBox x:Name="Dev" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Enable Developer" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,10,0" Height="20" ></TextBlock>
                    <TextBlock Text="Enables dev options and console commands" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,0,0" Height="16" />
                </Grid>
                <Grid Grid.Row="2" Grid.Column="0">
                    <CheckBox x:Name="ConsoleShow" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Show Console" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,10,0" Height="20" ></TextBlock>
                    <TextBlock Text="Show external game console" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,10,0" Height="16" />
                </Grid>
                <Grid Grid.Row="3" Grid.Column="0">
                    <CheckBox x:Name="ColoredCon" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Color Console" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,10,0" Height="20" ></TextBlock>
                    <TextBlock Text="Enable Colored game console" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,10,0" Height="16" />
                </Grid>
                <Grid Grid.Row="4" Grid.Column="0">
                    <CheckBox x:Name="PacketsEncrypt" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Encrypt Packets" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Toggles encryption for game packets" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,0,0" Height="16" />
                </Grid>
                <Grid Grid.Row="5" Grid.Column="0">
                    <CheckBox x:Name="PacketsQueued" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Queued Packets" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Sends packets using a priority thread" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,10,0" Height="16" />
                </Grid>
                <Grid Grid.Row="6" Grid.Column="0">
                    <CheckBox x:Name="RandomNetKey" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Random Netkey" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Generate a new random net key on launch" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,5,0" Height="16" />
                </Grid>
                <Grid Grid.Row="7" Grid.Column="0">
                    <CheckBox x:Name="NoTimeout" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="No Timeout" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Prevents disconnection for lost connections" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,6,0" Height="16" />
                </Grid>
                <Grid Grid.Row="8" Grid.Column="0">
                    <CheckBox x:Name="NoAsync" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="No Async" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Disables all asynchronous game processes" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,7,0" Height="16" />
                </Grid>
                <Grid Grid.Row="9" Grid.Column="0">
                    <CheckBox x:Name="Offline" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="52,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Offline Mode" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="111,0,87,0" Height="20" ></TextBlock>
                    <TextBlock Text="Disables access to any online features" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="111,38,7,0" Height="16" />
                </Grid>
                <Grid Grid.Row="2" Grid.Column="1">
                    <ComboBox x:Name="MapCmb" ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle}" SelectedIndex="0" HorizontalAlignment="Left" Margin="32,0,0,0" ShouldPreserveUserEnteredPrefix="True" Height="30" Width="275" Foreground="{StaticResource ComboTextColor}" Cursor="Hand" />
                    <TextBlock Text="Map" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,90,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="2" Grid.Column="2">
                    <ComboBox x:Name="PlaylistCmb" ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle}" SelectedIndex="0" HorizontalAlignment="Left" Margin="32,0,0,0" ShouldPreserveUserEnteredPrefix="True" Height="30" Width="275" Foreground="{StaticResource ComboTextColor}" Cursor="Hand" MaxDropDownHeight="1" />
                    <TextBlock Text="Playlist" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,90,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="3" Grid.Column="1">
                    <TextBox x:Name="PlaylistsFile" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="playlists_r5_patch.txt" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" Cursor="IBeam" KeyDown="TextBox_PreviewKeyDown" />
                    <TextBlock Text="Playlists file" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,29,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="3" Grid.Column="2">
                    <TextBox x:Name="HostName" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" Cursor="IBeam" KeyDown="TextBox_PreviewKeyDown" />
                    <TextBlock Text="Host Name" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,61,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="1" Grid.Column="1">
                    <ComboBox x:Name="Mode" ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle}" SelectedIndex="0" HorizontalAlignment="Left" Margin="32,0,0,0" ShouldPreserveUserEnteredPrefix="True" Height="30" Width="275" Foreground="{StaticResource ComboTextColor}" Cursor="Hand">
                        <ComboBoxItem Content="Host" />
                        <ComboBoxItem Content="Server" />
                        <ComboBoxItem Content="Client" />
                    </ComboBox>
                    <TextBlock Text="Mode" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,90,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="1" Grid.Column="2">
                    <ComboBox x:Name="VisibilityCmb" ItemContainerStyle="{DynamicResource ComboBoxItemContainerStyle}" SelectedIndex="0" HorizontalAlignment="Left" Margin="32,0,0,0" ShouldPreserveUserEnteredPrefix="True" Height="30" Width="275" Foreground="{StaticResource ComboTextColor}" Cursor="Hand">
                        <ComboBoxItem Content="Offline" />
                        <ComboBoxItem Content="Hidden" />
                        <ComboBoxItem Content="Public" />
                    </ComboBox>
                    <TextBlock Text="Visibility" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,90,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="6" Grid.Column="2">
                    <TextBox x:Name="MaxFPS" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="0" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox2_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Max FPS" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,90,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="6" Grid.Column="1">
                    <TextBox x:Name="ReservedCores" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="-1" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Reserved Cores" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,10,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="5" Grid.Column="2">
                    <TextBox x:Name="AffinityProc" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="0" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox2_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Processor Affinity" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,10,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="5" Grid.Column="1">
                    <TextBox x:Name="ThreadsWorker" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="-1" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Worker Threads" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,22,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="8" Grid.Column="1">
                    <TextBox x:Name="WidthRes" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox2_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Resolution Width" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,10,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="8" Grid.Column="2">
                    <TextBox x:Name="HeightRes" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="275" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="32,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Foreground="{StaticResource TextBoxTextColor}" FontSize="16" PreviewTextInput="NumericTextBox2_PreviewTextInput" PreviewKeyDown="NumericTextBox_PreviewKeyDown" Cursor="IBeam" />
                    <TextBlock Text="Resolution Height" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="312,0,10,0" Height="20" ></TextBlock>
                </Grid>
                <Grid Grid.Row="9" Grid.Column="1">
                    <CheckBox x:Name="Windowed" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="36,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Windowed" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="99,0,99,0" Height="20" ></TextBlock>
                    <TextBlock Text="Enabled windowed mode" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="99,40,22,0" Height="16" />
                </Grid>
                <Grid Grid.Row="9" Grid.Column="2">
                    <CheckBox x:Name="Borderless" Style="{StaticResource SwitchStyle}" Height="27" Width="50" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="36,0,0,0" Cursor="Hand" />
                    <TextBlock Text="Borderless" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="99,0,99,0" Height="20" ></TextBlock>
                    <TextBlock Text="Enable borderless windowed mode" Foreground="{StaticResource AccentTextColor}" FontFamily="Bahnschrift Light" VerticalAlignment="Top" Margin="99,40,22,0" Height="16" />
                </Grid>
                <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
                    <TextBox x:Name="CommandLine" Style="{DynamicResource AdvancedTextBox}" Height="30" Width="755" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="31,0,0,0" Background="{StaticResource TextBoxColorNormal}" BorderBrush="{StaticResource TextBoxBorderColor}" Text="" Foreground="White" FontSize="16" Cursor="IBeam" KeyDown="TextBox_PreviewKeyDown" />
                    <TextBlock Text="Command Line" TextWrapping="Wrap" Foreground="{StaticResource PrimaryTextColor}" FontSize="16" FontFamily="Bahnschrift SemiBold" VerticalAlignment="Center" Margin="791,0,16,0" Height="20" ></TextBlock>
                </Grid>
            </Grid>
            <Button Content="Focusable Button" Visibility="Collapsed" x:Name="HiddenFocusableButton" />
        </Grid>
    </Border>
</UserControl>